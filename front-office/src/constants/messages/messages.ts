import { Messages } from './interfaces';
import { HomePage } from './pages';
import { Footer, Heading } from './global';

export const MESSAGES: Messages = {
  global: {
    heading: Heading,
    footer: Footer,
  },
  pages: {
    home: HomePage,
  },
};
