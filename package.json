{
  "name": "test",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "front-office",
    "back-office",
    "backend",
    "content-generator"
  ],
  "scripts": {
    "start:dev": "npm -s run tools && npm -s run generate:content && concurrently \"yarn workspace backend start:dev\" \"yarn workspace front-office dev\" \"yarn workspace back-office dev\"",
    "generate:content": "yarn workspace content-generator start ",
    "tools": "node ./.lyttle_tools/src/app/core/app.js",
    "tools:version:update": "node ./.lyttle_tools/src/app/version-manager/version.js && chmod ug+x ./.git/hooks/*",
    "tools:breaking:accept": "node ./.lyttle_tools/src/app/breaking/accept.js",
    "heroku-postbuild": "npm -s run generate:content && yarn workspace backend build && y yarn workspace front-office build && yarn workspace back-office build"
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
